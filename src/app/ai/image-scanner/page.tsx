"use client";

import { Suspense } from "react";
import ImageScanner from "@/components/tools/ImageScanner";

export default function ImageScannerPage() {
  return <Suspense><ImageScanner /></Suspense>;
}
